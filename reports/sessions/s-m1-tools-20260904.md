# Session s-m1-tools-20260904

状态：已完成（代码/本地验收；真实服务联调另立待办）
负责人：DeepSeek Harness (dsh)
分支：`dsh/m1-tool-skill-completion-20260904`
基线 commit：`79973082535f459f7c0be03096c654d233fe99ab`（origin/main，执行时最新）
范围：按交接包 `handoff/m1-tool-skill-completion-20260904/` 完善 M1 Tool/Skill：17 个 M1 Tool 的真实 HTTP/Adapter 能力、`yunpai-m1-document-parser` 全部 17 Tool 操作映射、`ingest_document` 本地 fixture 加固、GB10 M1 HTTP 模块启用、M1 定向测试与验收记录。
认领路径：`src/yunpai_langgraph/registry.py`、`skills.py`、`agents.py`、`workers.py`、新增 `m1_tooling.py`/`m1_http_adapter.py`、`ops/gb10/start_backend.sh`、M1 定向测试、本任务项目账本段落。
开始时间：2026-09-04 21:00 +08:00

## 当前工作

- 已完成：读取全部交接文档并校验 sources SHA256SUMS（OK）；fetch 最新 origin/main（7997308）；新建独立 worktree 与分支 `dsh/m1-tool-skill-completion-20260904`；基线审计（114 Tool / M1 17 / default bound 45，其中 M1 仅 ingest_document 本地 fixture；Skill 声明 3 Tool）；基线 pytest 69 passed。
- 进行中：T8 M1 服务 API 面勘察、M1 Skill/Gate 接线勘察（已回）、M1 常量与操作映射落地。
- 下一步：M1 HTTP Adapter 实现、registry/skill/gate 接线、M1 定向测试与全套回归。

## 时间线

### 2026-09-04 21:00

- 计划修改：建立本 session 路径认领和实时报告，登记任务状态。
- 实际修改：创建 claim 和 session 报告；创建 `src/yunpai_langgraph/m1_tooling.py`（M1 工具清单、Skill 操作映射、只读/写入操作集合）。
- 文件：`.coordination/claims/s-m1-tools-20260904.md`、`reports/sessions/s-m1-tools-20260904.md`、`src/yunpai_langgraph/m1_tooling.py`。
- 验证：GitLab `origin/main` 基线 `79973082535f459f7c0be03096c654d233fe99ab`；`origin/dev` `a3384379…`；基线完整 pytest `69 passed`（退出 0）。
- 风险/阻塞：真实 M1 独立服务（MinerU/Instructor/PostgreSQL/Neo4j）与数据库读回条件未取得，只声明代码/本地 HTTP mock 通过；生产验收状态另行如实记录。
- 证据：交接包 `handoff/m1-tool-skill-completion-20260904/`、本报告。

### 2026-09-04 21:58

- 计划修改：实现 M1 专用 HTTP Adapter、Registry 绑定、Skill 17 Tool 映射与 Gate 分类、本地 fixture 加固。
- 实际修改：
  - 新增 `src/yunpai_langgraph/m1_tooling.py`（17 Tool 清单、Skill 操作映射 19 ops、只读/写入集合、Adapter 绑定入口）。
  - 新增 `src/yunpai_langgraph/m1_http_adapter.py`（`X-Tenant-ID` 双头传播与 MISSING_TENANT 失败关闭、`X-Actor-ID`/`X-Actor-Roles` 仅受信任来源、multipart 上传、202 有界轮询与显式 pending、稳定错误映射、`search_m1_orders` 属性过滤前向兼容）。
  - `registry.py`：default registry 绑定 M1 Adapter（17/17，ingest_document 保留本地 fixture）；runtime HTTP 用专用 M1 Adapter 覆盖通用绑定。
  - `skills.py`：`yunpai-m1-document-parser` 声明全部 17 Tool，operation map 改由 `m1_tooling.M1_SKILL_OPERATION_MAP` 驱动。
  - `agents.py`：M1 只读 Skill 操作白名单 = `M1_READ_ONLY_SKILL_OPERATIONS`（13 个查询 op），archive/review/report/parse 走授权 Gate。
  - `workers.py`：`m1_parse` 加固为显式 fixture（`provider=local_fixture`）；非 JSON/无预解析结构一律 `LOCAL_FIXTURE_UNSUPPORTED_FORMAT` 失败关闭，不冒充完整 M1 解析。
  - `ops/gb10/start_backend.sh`：默认 HTTP 模块改为 `m1,m2` 并给出 `M1_URL`；新增 `ops/m1/README.md` 服务接入文档。
  - 更新 `tests/test_registry.py`（61 bound）与 `tests/test_api.py`（bound_tools 61）；新增 9 个 M1 定向测试文件与 `tests/m1_fake_http.py` 假 HTTP 后端。
- 文件：上述路径 + `.coordination/claims/s-m1-tools-20260904.md`、`reports/sessions/s-m1-tools-20260904.md`。
- 验证：compileall OK；M1 定向测试全绿；完整 pytest `117 passed, 2 skipped`（2 skipped 为真实服务 opt-in 用例）；Registry default `114 specs / 61 bound`（m1 17/17），HTTP runtime `112 bound`（m1 17/17，专用 Adapter）。
- 风险/阻塞：真实 M1 独立服务（M1_URL、X-API-Key、PostgreSQL/Neo4j、GPU MinerU/Instructor）未提供，只能声明“代码/本地 HTTP mock 通过”；`order_type` 与订单属性过滤服务端未实现，属性过滤当前为 Adapter 客户端过滤。
- 证据：本报告、M1 定向测试与完整 pytest 输出。

### 2026-09-04 22:25

- 计划修改：将本地 HTTP mock 升级为对真实运行的 T8 M1 独立服务的本地 HTTP 联调。
- 实际修改：无代码改动；用 python3.12 独立 venv 安装 T8 M1 基础依赖（不含 ocr/docling extras），以 SQLite+memory graph 在 127.0.0.1:18081 启动真实服务；用编排器专用 M1 Adapter 直连真实 HTTP 探测。
- 文件：`ops/m1/README.md`（补充本地真实运行联调记录）。
- 验证（真实服务，非 mock）：/health 与 /ready 可达并如实报告 `degraded`/`not_ready`（mineru_shadow 未配置、无 GPU/模型层）；list_m1_tasks/review_queue/knowledge stats/search 空库正确返回；真实 XLSX multipart 上传 → task b1e064a04ce9 进入 `needs_review`（HITL 候选，不伪造成功）；get_m1_task/get_m1_document 回读 m1.document.v2；review 队列含该任务；跨租户 othertenant 读同任务 → 404。
- 结论：真实服务本地运行联调通过（只读端点/入库/租户隔离/候选边界）；完整生产验收仍不通过（缺 GPU/MinerU/Instructor、PostgreSQL/Neo4j 与真实样本）。
- 证据：/tmp/m1run/uvicorn.log、探针输出、E-M1-LOCAL-001。

## 交接

- 提交：代码与测试提交 `8a9794d5e7f2e92acbb122692cc66262af59ddd3`（m1: complete tool and skill integration，Session: s-m1-tools-20260904）；账本记录提交随后推送。
- 分支：`dsh/m1-tool-skill-completion-20260904`（基线 origin/main `7997308`）。
- 验证：完整 pytest `117 passed, 2 skipped`（退出 0）；compileall、`git diff --check` 通过；Registry default `61 bound`（M1 17/17）、HTTP runtime `112 bound`（M1 17/17 专用 Adapter）。
- 未完成事项：真实 M1 独立服务完整验收（/health、/ready 达 ok、脱敏真实样本逐格式上传、PostgreSQL/Neo4j 回读、双租户数据交叉拒绝、GPU MinerU/Instructor 模型依赖）需模型与数据库条件；本地真实服务运行联调（SQLite+memory、只读与候选边界）已执行并记录 E-M1-LOCAL-001；`order_type` 与订单属性过滤服务端实现待 M1 服务补丁（当前 Adapter 客户端过滤兼容）。
- 接手人：集成负责人（zhb）审查后合入 `dev`。
