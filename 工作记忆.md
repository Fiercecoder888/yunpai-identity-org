# 工作记忆

## 高优先级记忆（P0）

- GitLab Web：`http://192.168.110.22:8181`
- GitLab SSH：`ssh://git@192.168.110.22:2222`
- 当前仓库：`yunpaiadmin/yunpai-gragh0903`
- 本地远端：`origin`，对应上述仓库；开发集成分支为 `dev`，`main` 仅用于集成/发布基线。
- GitLab 管理操作可通过 `wjc@192.168.110.22`、公司 SSH 密钥和免密 `sudo` 调用 `gitlab-rails runner`；优先生成短期一次性管理员 API Token，操作完成后立即撤销并复核。密码、Token、私钥不写入仓库记忆。
- 新增协作者应在目标项目成员页确认用户名及 access level；高风险权限变更需人工确认并保留操作结果。

## 来源与核验

- 2026-09-04：从当前仓库 `git remote -v` 核对 SSH 远端与项目路径。
- 2026-09-04：从既有 `/Users/murkydoubloon45/Desktop/yunpai/工作记忆.md`、`AGENTS.md`、`INFRASTRUCTURE_MEMORY.md` 和交接文档整理 GitLab/网络约定。
- 2026-09-04：实测 `192.168.110.22:8181` 可打开 GitLab Community Edition 登录页；当前未登录。

## GitLab 用户与项目权限操作

### chendongsheng（2026-09-04）

- 已创建用户 `chendongsheng`（Chen Dongsheng），默认邮箱 `chendongsheng@yunpai.local`，user_id `20`。
- 用户状态为 active、confirmed、非 admin；随机初始密码已设置并校验，`password_expires_at` 已设置为创建时间，要求首次登录修改密码。密码明文只在本次交付中提供，不写入记忆。
- 已作为直接成员加入 `yunpaiadmin/yunpai-gragh0903`，member_id `52`，`Maintainer`（access_level `40`）。
- 操作使用短期一次性管理员 API Token；结束后 Rails 复核同名活动临时 Token 数量为 `0`。
- GitLab 19 兼容性：`Project.find_by_full_path!` 不可用，应使用 `Project.find_by_full_path(...)` 并显式检查空值。
- 2026-09-04：历史记忆已确认 GitLab 管理路径为服务器 SSH + Rails 一次性管理员 API Token，不依赖或复制网页管理员密码；凭据内容不得写入仓库。

## GitLab 连接记忆（2026-09-04）

- 已从其他 Yunpai 项目和本机 SSH 配置确认：GitLab `192.168.110.22:2222` 使用公司密钥文件 `~/.ssh/id_ed25519_company`，用户名为 `git`；不记录私钥内容。
- 推荐命令：`GIT_SSH_COMMAND='ssh -i ~/.ssh/id_ed25519_company -o IdentitiesOnly=yes -o BatchMode=yes -o ConnectTimeout=10' git fetch origin --prune`。
- 当前仓库远端：`ssh://git@192.168.110.22:2222/yunpaiadmin/yunpai-gragh0903.git`。
- 2026-09-04 实测 `git ls-remote` 鉴权成功，远端 `main` 为 `9cb5a0e4e2cc05cdc7f59585460ecfa9fe0e8de5`；直接执行普通 shell 命令会被 Git shell 拒绝，属于正常行为。
- 2026-09-04：确认本地 `main` 原提交 `59e9bfe` 是远端 main 的祖先，已安全快进本地 `main` 到上述 SHA；当前开发工作仍在 `dev`。
- 连接失败时先检查密钥权限、`IdentitiesOnly=yes` 和 `known_hosts`，不得把密码、Token 或私钥写入项目记忆。

## GB10 Qwen 35B 调用记忆（2026-09-04）

### 稳定连接参数

- API 格式：OpenAI-compatible。
- 服务模型：`qwen3.6-35b-a3b-fp8-gpu0-200k`。
- 最大上下文：`65536`。
- GB10 本机首选地址：`http://127.0.0.1:18085/v1`。
- GB10 局域网地址：`http://192.168.110.19:18085/v1`。
- 可解析 `gb10` 主机名的环境：`http://gb10:18085/v1`。
- `18085` 是对应用的鉴权入口；实际 vLLM 后端监听 `127.0.0.1:18095`，业务应用不直连 `18095`。
- API Key 已由 GB10 管理。编排服务从 `/home/soft/yunpai/dev-39085/config/deploy.env` 读取 `ORCH_LLM_API_KEY`，再注入 `QWEN_API_KEY`；不将值写入仓库、命令记录或测试报告。

### GB10 本机调用示例

```bash
set -a
source /home/soft/yunpai/dev-39085/config/deploy.env
set +a

curl -fsS http://127.0.0.1:18085/v1/chat/completions \
  -H "Authorization: Bearer ${ORCH_LLM_API_KEY}" \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "qwen3.6-35b-a3b-fp8-gpu0-200k",
    "messages": [{"role": "user", "content": "Reply exactly: GB10-QWEN-OK"}],
    "temperature": 0,
    "max_tokens": 128,
    "chat_template_kwargs": {"enable_thinking": false}
  }'
```

项目后端使用以下环境变量：

```bash
export QWEN_ROUTER_ENABLED=true
export QWEN_BASE_URL=http://127.0.0.1:18085/v1
export QWEN_MODEL=qwen3.6-35b-a3b-fp8-gpu0-200k
export QWEN_API_KEY="${ORCH_LLM_API_KEY}"
```

### 实测结果

- `GET /v1/models`：GB10 本机、局域网 IP 和 `gb10` 主机名三种入口均返回 HTTP 200，且均列出目标模型。
- `POST /v1/chat/completions`：返回 `finish_reason=stop`、正文 `GB10-QWEN-OK`，请求模型与响应模型一致。
- 过小的 `max_tokens`（实测 `16`）可被 thinking 消耗并导致正文为空；稳定的确定性调用建议设置 `max_tokens >= 128`，并显式传递 `chat_template_kwargs.enable_thinking=false`。
- 未携带 Bearer Key 访问局域网 `/v1/models` 返回 HTTP 401，鉴权已生效。

## GitLab22 FRP 与 Tailscale 记忆（2026-09-04）

- 目标主机：`gitlab` / GitLab22，LAN `192.168.110.22`，Tailscale `100.127.40.100`，Tailscale 主机名 `gitlab22`。
- GitLab 本地入口保持不变：Web `http://192.168.110.22:8181`，Git SSH `git@192.168.110.22:2222`；GitLab Omnibus `external_url` 为 `http://192.168.110.22:8181`，`gitlab_shell_ssh_port=2222`。
- Tailscale 已安装并持久化：`tailscaled.service` 为 `enabled/active`，节点状态 `Running`；推荐远程入口为 `http://100.127.40.100:8181` 和 `git@100.127.40.100:2222`。本机实测 `tailscale ping 100.127.40.100` 经 LAN 直连，延迟约 8ms。
- FRP 客户端已安装并持久化：`frpc.service` 为 `enabled/active`；二进制 `/usr/local/frp/frpc`，配置 `/usr/local/frp/frpc.toml`，证书目录 `/usr/local/frp/cert/`。配置连接 `linkdown.bitnp.asia:7000`，启用 TLS，token 仅保存在服务器配置中，不写入记忆。
- FRP 映射：`gitlab-web` 使用 `https2http`，公网 `https://linkdown.bitnp.asia:6001` 转发到 `127.0.0.1:8181`；`gitlab-ssh` 使用 TCP，公网 `linkdown.bitnp.asia:6002` 转发到 `127.0.0.1:2222`。公网 443 不是本 GitLab 入口，勿省略 `:6001`/`:6002`。
- 2026-09-04 对 FRP 配置执行 `frpc verify` 并重启服务；两个 proxy 均报告 `start proxy success`，控制连接已建立。因配置含认证 token，`/usr/local/frp/frpc.toml` 已收紧为 `0600 root:root`；原文件备份在服务器 `/root/frpc.toml.bak-pre-permissions-20260904-145836`，同样为 `0600 root:root`。
- 2026-09-04 脱敏验收：LAN Web、Tailscale Web、FRP Web 均 HTTP 200；LAN/Tailscale/FRP 三个 Git SSH 入口的 `git ls-remote` 均返回同一 HEAD `79973082535f459f7c0be03096c654d233fe99ab`；未修改业务代码、GitLab 数据或仓库内容。
- Git 操作优先级：同网段使用 LAN；跨网首选 Tailscale；仅在 Tailscale 不可用或过慢时使用 FRP。FRP 控制链路受公网动态 IP/路由器状态影响，出现超时先检查 `frpc.service`、`linkdown.bitnp.asia:7000` 和 OpenWrt 防火墙，再判断 GitLab 服务故障。
